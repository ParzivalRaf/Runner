using UnityEngine;

/// <summary>
/// Метка «по этому можно бежать».
///
/// Игрок каждый кадр пускает луч вниз и ищет ближайшую поверхность с этой
/// меткой. Пол чанка, крыша поезда, площадка эстакады — всё это одно и то же
/// с точки зрения кода, отличается только высотой.
///
/// Почему метка-компонент, а не слой (Layer): слоёв в Unity всего 32, они
/// живут в настройках проекта и их легко забыть проставить на новом префабе —
/// а забытый слой проявится как «игрок проваливается сквозь поезд» и будет
/// долго искаться. Компонент видно прямо на объекте.
///
/// Куда вешать: на объект с обычным коллайдером (НЕ триггером).
/// Триггеры луч игнорирует намеренно: препятствия — это триггеры,
/// и бежать по ним нельзя.
/// </summary>
public class GroundSurface : MonoBehaviour
{
}
